import { CertificacionModel } from './certificacion.model';

describe('CertificacionModel', () => {
  it('should create an instance', () => {
    expect(new CertificacionModel()).toBeTruthy();
  });
});
