import { TransaccionesModel } from './transacciones.model';

describe('TransaccionesModel', () => {
  it('should create an instance', () => {
    expect(new TransaccionesModel()).toBeTruthy();
  });
});
